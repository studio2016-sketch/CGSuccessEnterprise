import {notFound} from 'next/navigation';import {services} from './services';import type {Metadata} from 'next';type Slug=keyof typeof services;export function generateStaticParams(){return Object.keys(services).map(slug=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const s=services[slug as Slug];return s?{title:s.title,description:s.intro,alternates:{canonical:'/expertise/'+slug},openGraph:{title:s.title+' | CG Enterprises',description:s.intro,url:'/expertise/'+slug}}:{title:'Expertise'}}export default async function Service({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services[slug as Slug];if(!s)notFound();return <main className="interior serviceLanding"><a href="/expertise">← ALL EXPERTISE</a><h1>{s.headline}</h1><p>{s.intro}</p><section><h2>From vision to a clear plan.</h2><p>{s.overview}</p></section><section><h2>Designed around your objectives.</h2><ul className="offeringList serviceDeliverables">{s.deliverables.map(x=><li key={x}>{x}</li>)}</ul></section><section><h2>A thoughtful engagement, from beginning to end.</h2><div className="editorialCards">{s.process.map(([title,description])=><div className="editorialCard" key={title}><h2>{title}</h2><p>{description}</p></div>)}</div></section><p className="formNote">Final deliverables and timelines are defined in each client engagement.</p><a className="goldButton" href="/contact">START A PROJECT CONVERSATION ↗</a></main>}