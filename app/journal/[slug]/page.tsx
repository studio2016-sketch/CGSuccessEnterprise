import {notFound} from 'next/navigation';import {articles} from './articles';import type {Metadata} from 'next';type Slug=keyof typeof articles;export function generateStaticParams(){return Object.keys(articles).map(slug=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const a=articles[slug as Slug];return a?{title:a.title,description:a.deck,alternates:{canonical:'/journal/'+slug},openGraph:{type:'article',title:a.title,description:a.deck,url:'/journal/'+slug}}:{title:'Journal'}}export default async function Article({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const a=articles[slug as Slug];if(!a)notFound();return <main className="interior articlePage"><a href="/journal">← THE CG JOURNAL</a><p className="eyebrow">{a.category} / CG PERSPECTIVES</p><h1>{a.title}</h1><p>{a.deck}</p><div className="articleBody">{a.sections.map(([heading,body])=><section key={heading}><h2>{heading}</h2><p>{body}</p></section>)}</div><div className="articleCta"><h2>Ready to put these ideas to work?</h2><p>Let's talk about the challenges and opportunities in your organization.</p><a className="goldButton" href="/contact">START A CONVERSATION ↗</a></div></main>}